tolerance_mad: 1.3
target_ratio: auto
bg_threshold: 245
padding_pct: 5.0
recenter: true
min_confidence: 0.8
max_upscale: 1.2
output_canvas:
- 600
- 800
# Filters — images matching these get copied into skipped/ with a reason tag,
# not processed and not sent to review.
skip_lifestyle: true
lifestyle_bg_threshold: 0.85
